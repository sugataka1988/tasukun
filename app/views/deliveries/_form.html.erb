<%= form_with(model: delivery, local: true) do |form| %>
  <% if delivery.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(delivery.errors.count, "error") %> prohibited this delivery from being saved:</h2>

      <ul>
        <% delivery.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :shipping_method %>
    <%= form.text_field :shipping_method %>
  </div>

  <div class="field">
    <%= form.label :delivery_company_name %>
    <%= form.text_field :delivery_company_name %>
  </div>

  <div class="field">
    <%= form.label :delivery_carrier_phone_number %>
    <%= form.number_field :delivery_carrier_phone_number %>
  </div>

  <div class="field">
    <%= form.label :delivery_company_address %>
    <%= form.text_field :delivery_company_address %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
