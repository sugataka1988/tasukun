<%= form_with(model: task, local: true) do |form| %>
  <% if task.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(task.errors.count, "error") %> prohibited this task from being saved:</h2>

      <ul>
        <% task.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :reception_date %>
    <%= form.datetime_select :reception_date %>
  </div>

  <div class="field">
    <%= form.label :task_content %>
    <%= form.text_area :task_content %>
  </div>

  <div class="field">
    <%= form.label :number_of_requests %>
    <%= form.number_field :number_of_requests %>
  </div>

  <div class="field">
    <%= form.label :desired_completion_date %>
    <%= form.datetime_select :desired_completion_date %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
