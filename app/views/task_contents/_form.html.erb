<%= form_with(model: task_content, local: true) do |form| %>
  <% if task_content.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(task_content.errors.count, "error") %> prohibited this task_content from being saved:</h2>

      <ul>
        <% task_content.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :task_content %>
    <%= form.text_area :task_content %>
  </div>

  <div class="field">
    <%= form.label :desired_completion_date %>
    <%= form.datetime_select :desired_completion_date %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
